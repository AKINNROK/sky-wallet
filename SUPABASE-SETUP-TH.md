# ☁️ เปิดซิงก์ข้ามเครื่องด้วย Supabase (ฟรี) — คู่มือฉบับไม่เคยทำมาก่อน

ทำครั้งเดียว ~10 นาที แล้วมือถือ ไอแพด คอม จะเห็นข้อมูลชุดเดียวกันตลอดไป
**ไม่ต้องเขียนโค้ดเลย** แค่ก๊อป-วาง 2 ค่า กับกดรัน SQL 1 ครั้ง

---

## ขั้นที่ 1 — สมัคร Supabase

1. เข้า **https://supabase.com** → กด **Start your project**
2. เข้าสู่ระบบด้วย **GitHub** (บัญชีเดิมที่ทำ repo ได้เลย) หรือ Google
3. กด **New project**
   - **Name:** `sky-wallet`
   - **Database Password:** กด Generate แล้ว **ก๊อปเก็บไว้ที่ปลอดภัย** (ใช้ไม่บ่อย แต่หายแล้วยุ่ง)
   - **Region:** เลือก **Southeast Asia (Singapore)** — ใกล้ไทยที่สุด เร็วที่สุด
   - **Plan:** Free
4. กด **Create new project** → รอ ~2 นาที ให้ขึ้นว่าพร้อมใช้งาน

---

## ขั้นที่ 2 — สร้างตารางเก็บข้อมูล (ก๊อป-วางอย่างเดียว)

1. เมนูซ้าย เลือก **SQL Editor** (ไอคอน `>_`)
2. กด **+ New query**
3. **ก๊อปโค้ดข้างล่างทั้งก้อน** วางลงไป แล้วกด **Run** (หรือ Ctrl+Enter)

```sql
-- ตารางเก็บข้อมูล Sky Wallet ของแต่ละคน
create table if not exists public.wallet_data (
  user_id    uuid primary key references auth.users(id) on delete cascade,
  data       jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now()
);

-- เปิดระบบความปลอดภัย: ห้ามใครเห็นข้อมูลของคนอื่น
alter table public.wallet_data enable row level security;

drop policy if exists "read own row"   on public.wallet_data;
drop policy if exists "insert own row" on public.wallet_data;
drop policy if exists "update own row" on public.wallet_data;

create policy "read own row"   on public.wallet_data
  for select using (auth.uid() = user_id);

create policy "insert own row" on public.wallet_data
  for insert with check (auth.uid() = user_id);

create policy "update own row" on public.wallet_data
  for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

4. ต้องขึ้นข้อความเขียว **Success. No rows returned** = เรียบร้อย ✅
   (รันซ้ำกี่รอบก็ได้ ไม่พัง)

---

## ขั้นที่ 3 — ปิดการยืนยันอีเมล (ไม่งั้นต้องรอเมลทุกครั้ง)

1. เมนูซ้าย **Authentication** → **Sign In / Providers**
2. กดที่ **Email**
3. ปิดสวิตช์ **Confirm email** ให้เป็นสีเทา
4. กด **Save**

> ถ้าไม่ปิด ตอนสมัครจะต้องเปิดอีเมลกดยืนยันก่อนถึงใช้ได้ (ก็ได้เหมือนกัน แต่ยุ่งกว่า)

---

## ขั้นที่ 4 — ก๊อป 2 ค่าจาก Supabase

1. เมนูซ้ายล่าง **Project Settings** (รูปเฟือง) → **API Keys** (หรือหัวข้อ **Data API**)
2. ก๊อป 2 ค่านี้เก็บไว้

| ชื่อในหน้าเว็บ | หน้าตา | เอาไปทำอะไร |
|---|---|---|
| **Project URL** | `https://abcdefgh.supabase.co` | ที่อยู่ฐานข้อมูลของคุณ |
| **anon** / **public** key | `eyJhbGciOiJIUzI1NiIs...` (ยาวมาก) | กุญแจสำหรับเข้าใช้ |

> ⚠️ **ห้ามใช้ `service_role` key เด็ดขาด** — ตัวนั้นเปิดได้ทุกอย่าง
> ส่วน `anon` key แปะในแอปได้ปลอดภัย เพราะมี RLS จากขั้นที่ 2 คุมอยู่

---

## ขั้นที่ 5 — ใส่ค่าลงแอป (เครื่องแรก)

1. เปิด Sky Wallet → กด **ไอคอนก้อนเมฆ ☁️** มุมขวาบน
2. เลื่อนหาหัวข้อ **☁️ ซิงก์ข้ามเครื่อง (Supabase)** → กดแถบ **"ยังไม่ได้ตั้งค่าคลาวด์"**
3. วาง **Project URL** และ **anon public key** → กด **บันทึกและเชื่อมต่อ**
4. หน้าถัดไปใส่ **อีเมล + รหัสผ่าน** (ตั้งใหม่เองได้เลย อย่างน้อย 6 ตัว) → กด **สมัครใหม่**
5. ขึ้น ✅ **เชื่อมต่อแล้ว** และจุดบนไอคอนเมฆเปลี่ยนเป็น **สีเขียว** = เสร็จ!
   ข้อมูลที่มีอยู่ในเครื่องจะถูกอัปขึ้นคลาวด์ให้อัตโนมัติ

---

## ขั้นที่ 6 — เครื่องที่ 2 (มือถือ / คอม / ไอแพด)

1. เปิดลิงก์แอปเดิมในเครื่องที่ 2
2. ไอคอนเมฆ ☁️ → **ยังไม่ได้ตั้งค่าคลาวด์** → วาง **URL + key ชุดเดิม**
3. ใส่ **อีเมล + รหัสผ่านเดิม** → กด **เข้าสู่ระบบ** (ไม่ใช่สมัครใหม่)
4. ข้อมูลจากเครื่องแรกไหลลงมาทันที ✨

---

## ใช้งานประจำวัน — ไม่ต้องกดอะไรอีกเลย

| เหตุการณ์ | ระบบทำอะไร |
|---|---|
| แก้ไขรายการ | บันทึกลงเครื่อง + อัปขึ้นคลาวด์ให้เองใน ~2 วินาที |
| เปิดแอป / รีเฟรช | ดึงข้อมูลล่าสุดจากคลาวด์ |
| สลับกลับมาที่แอป | เช็กและดึงของใหม่ |
| เปิดแอปค้างไว้ | เช็กคลาวด์ให้ทุก 1 นาที |
| ไม่มีเน็ต | ใช้ต่อได้ปกติ พอมีเน็ตแล้วค่อยซิงก์ให้เอง |

**ปุ่มสำรอง 2 ปุ่ม** (ใช้เฉพาะตอนข้อมูลชนกัน)
- ⬆️ **อัปทับคลาวด์** — ยึดข้อมูลในเครื่องนี้เป็นหลัก
- ⬇️ **ดึงทับเครื่องนี้** — ยึดข้อมูลบนคลาวด์เป็นหลัก

---

## เรื่องที่ควรรู้

- **ฟรีจริง** — Supabase Free ให้ 500 MB ข้อมูลคุณใช้ไม่ถึง 1 MB ต่อให้บันทึกทุกวันหลายปี
- **โปรเจกต์ Free จะถูกพักถ้าไม่ใช้งาน 7 วัน** — แค่เข้า dashboard กด Restore ก็กลับมา ข้อมูลไม่หาย
- **ความปลอดภัย** — RLS จากขั้นที่ 2 ทำให้แต่ละบัญชีเห็นได้เฉพาะแถวของตัวเอง คนอื่นถึงมี anon key ก็อ่านข้อมูลคุณไม่ได้
- **ควรกด 💾 สำรองไฟล์ เก็บไว้เป็นระยะอยู่ดี** — คลาวด์ฟรีก็มีสิทธิ์มีปัญหาได้
- **ลืมรหัสผ่าน?** Supabase → Authentication → Users → จุดสามจุดข้างอีเมล → Reset password

---

## แก้ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| *"Project URL ต้องขึ้นต้นด้วย https://"* | ก๊อป URL มาไม่ครบ ต้องเป็น `https://xxxx.supabase.co` ไม่มี `/` ต่อท้าย |
| *"relation public.wallet_data does not exist"* | ยังไม่ได้รัน SQL ในขั้นที่ 2 |
| *"new row violates row-level security policy"* | Policy ไม่ครบ — กลับไปรัน SQL ขั้นที่ 2 ซ้ำอีกรอบได้เลย |
| สมัครแล้วขึ้นให้ยืนยันอีเมล | ยังไม่ได้ปิด **Confirm email** ในขั้นที่ 3 |
| *"Invalid login credentials"* | อีเมล/รหัสผ่านผิด หรือเครื่องนี้ควรกด **สมัครใหม่** เพราะยังไม่เคยมีบัญชี |
| จุดบนไอคอนเมฆยังเหลือง | ยังไม่ได้เข้าสู่ระบบ — กดไอคอนเมฆแล้วเข้าสู่ระบบ |
| *"โหลดไลบรารี Supabase ไม่สำเร็จ"* | ตอนตั้งค่าครั้งแรกต้องมีอินเทอร์เน็ต (โหลดไลบรารีจาก CDN) |
